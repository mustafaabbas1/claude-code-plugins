# <Project title>

<One-line short description — the project's tagline.>

## Outcome

One paragraph describing the end state in the world, for whom. Not a feature
list. What is true after this project succeeds that wasn't true before.

## Problem

What is broken or missing today, from the user's perspective. Why this is
worth doing now.

## Users

Short list of actor types this serves. Not personas — just who.

## Domain glossary

5–15 terms with one-line definitions each. The shared vocabulary that every
downstream skill (plans, issues, implementation) must use. Be precise — vague
terms here propagate downstream.

## Principles

3–7 opinionated stances that will guide future trade-offs. Each is a one-liner
that takes a side (e.g. "prefer batch over streaming", "single-tenant only",
"no client-side state for forms"). These anchor later ADRs.

## Out of scope

Explicit non-goals. Things this project will not try to do, even if tempting.
